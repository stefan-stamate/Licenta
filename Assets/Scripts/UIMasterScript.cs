using UnityEngine;
using System.Collections;

public class UIMasterScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
	/*
	public void DeleteObject(GameObject target){
		//use the custom defined function
		if (target.tag == "Wall")
			target.GetComponent<WallBehaviour> ().DeleteSelf ();
		else
			print ("Unidentified object encountered!");
	}

	public void ChangeMaterial(GameObject target){
	}
	*/
}
